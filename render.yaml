services:
  - type: web
    name: healthcare-ai-backend
    runtime: node
    buildCommand: npm install
    startCommand: node backend/server.js
    envVars:
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
